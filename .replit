modules = ["nodejs-20"]
[agent]
expertMode = true

[nix]
channel = "stable-25_05"

[workflows]
runButton = "Project"

[[workflows.workflow]]
name = "Project"
mode = "parallel"
author = "agent"

[[workflows.workflow.tasks]]
task = "workflow.run"
args = "Predictli App"

[[workflows.workflow]]
name = "Predictli App"
author = "agent"

[[workflows.workflow.tasks]]
task = "shell.exec"
args = "cd predictli-v5.0-beta && npm run dev:all"
waitForPort = 5000

[workflows.workflow.metadata]
outputType = "webview"

[[ports]]
localPort = 3000
externalPort = 3001

[[ports]]
localPort = 5000
externalPort = 80

[[ports]]
localPort = 32887
externalPort = 3000

[deployment]
deploymentTarget = "autoscale"
run = ["bash", "-c", "cd predictli-v5.0-beta && node dist/server.js & cd ui && npx serve -s dist -l 5000"]
build = ["bash", "-c", "cd predictli-v5.0-beta && npm install && npm run build"]
